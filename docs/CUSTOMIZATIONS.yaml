{
  "schema_version": 2,
  "format_note": "JSON-compatible YAML; parsed with Node.js JSON.parse to avoid a runtime dependency.",
  "upstream": {
    "repository": "https://github.com/decolua/9router",
    "ref": "v0.5.69",
    "sha": "eb712ca821f0ba6bc41043fbd14494c5af5daba5"
  },
  "reset": {
    "date": "2026-09-09",
    "branch": "task/azox-9router-reset-upstream-prefill-contrib",
    "baseline": "The branch starts directly at upstream SHA eb712ca8 and re-applies the reviewed scopes below.",
    "diff_counts": {
      "additive": 36,
      "modified": 33,
      "total": 69,
      "verify": "git diff --name-status v0.5.69 HEAD | awk '{print $1}' | sort | uniq -c"
    },
    "retired_old_fork_paths": 275,
    "note": "The previous fork differed at 340 paths. This reset intentionally removes the custom Responses/SSE conversion stack that caused content loss, duplicate Responses output, and InvalidStreamResponseError in live A/B tests."
  },
  "supported_customizations": {
    "claude_assistant_prefill": {
      "purpose": "Apply a target-format policy when a request ends with an assistant message: strip by default for Claude Messages, preserve only when x-9router-assistant-prefill is set to preserve, and treat a recognized upstream rejection as model compatibility rather than an account failure.",
      "primary_paths": [
        "open-sse/translator/concerns/assistantPrefillPolicy.js",
        "open-sse/translator/formats/claude.js",
        "open-sse/config/errorConfig.js",
        "open-sse/services/accountFallback.js",
        "open-sse/services/combo.js",
        "open-sse/utils/error.js",
        "src/sse/services/auth.js",
        "open-sse/handlers/chatCore.js"
      ],
      "tests": [
        "tests/translator/assistant-prefill-policy.test.js",
        "tests/unit/account-fallback-prefill.test.js",
        "tests/unit/combo-fusion.test.js",
        "tests/unit/combo-retry-after.test.js"
      ]
    },
    "contributor": {
      "purpose": "Invitation-based contributor onboarding, scoped OAuth completion, contributor sessions, admin invite management, and dashboard navigation.",
      "primary_paths": [
        "src/app/(dashboard)/dashboard/contributors/page.js",
        "src/app/api/contribute/oauth/[provider]/[action]/route.js",
        "src/app/api/contribute/session/route.js",
        "src/app/api/contributor-admin/invites/route.js",
        "src/app/contribute/page.js",
        "src/app/contribute/[token]/page.js",
        "src/app/contribute/TokenExchangeClient.js",
        "src/lib/contributor/session.js",
        "src/lib/contributor/store.js",
        "src/dashboardGuard.js",
        "src/shared/components/Header.js",
        "src/shared/components/OAuthModal.js",
        "src/shared/components/Sidebar.js",
        "src/shared/constants/customNavigation.js",
        "src/shared/utils/oauthOrigin.js"
      ],
      "tests": [
        "tests/unit/contributor-store.test.js",
        "tests/unit/contributor-session-origin.test.js",
        "tests/unit/contributor-oauth-guard.test.js",
        "tests/unit/contributor-atomic-completion.test.js",
        "tests/unit/contributor-atomic-sqlite.test.js",
        "tests/unit/contributor-proxy-atomic.test.js",
        "tests/unit/contributor-gitlab-oauth-ssrf.test.js",
        "tests/unit/oauth-register-session.test.js",
        "tests/unit/custom-navigation.test.js",
        "tests/unit/contributor-route-hardening.test.js",
        "tests/unit/contributor-secret.test.js"
      ]
    },
    "combo_import_export": {
      "purpose": "Authenticated combo export and validated atomic import with explicit conflict handling.",
      "primary_paths": [
        "src/app/(dashboard)/dashboard/import-export/page.js",
        "src/app/api/import-export/combos/route.js",
        "src/lib/db/repos/combosRepo.js",
        "src/lib/db/index.js",
        "src/lib/localDb.js"
      ],
      "tests": [
        "tests/unit/combo-import-export.test.js"
      ]
    }
  },
  "retained_dependency_closure": {
    "request_and_runtime_helpers": [
      "open-sse/config/runtimeConfig.js",
      "open-sse/providers/capabilities.js",
      "open-sse/services/modelCatalogResponse.js",
      "open-sse/translator/concerns/hostedToolPolicy.js",
      "open-sse/translator/schema/blocks.js",
      "open-sse/utils/abort.js",
      "open-sse/utils/requestBody.js"
    ],
    "oauth_and_connection_atomicity": [
      "src/app/api/oauth/[provider]/[action]/route.js",
      "src/lib/db/repos/connectionsRepo.js",
      "src/lib/oauth/providerHelpers.js",
      "src/lib/oauth/providers/gitlab.js",
      "src/lib/oauth/providers/xai.js",
      "src/lib/oauth/services/qoder.js",
      "src/lib/oauth/utils/server.js",
      "src/lib/oauth/utils/xaiHttp.js",
      "src/shared/utils/apiKey.js"
    ],
    "database_integrity": [
      "src/lib/db/adapters/sqljsAdapter.js",
      "src/lib/db/backup.js",
      "src/lib/db/repos/usageRepo.js"
    ],
    "standalone_build": [
      "Dockerfile",
      "next.config.mjs",
      "package-lock.json",
      "scripts/copy-standalone-assets.mjs",
      "tests/unit/standalone-assets.test.js"
    ],
    "rationale": "These files are retained because the supported features import their symbols or rely on their atomicity, bounded-request, OAuth-origin, database, or standalone-image contracts. Several are whole-file carryovers and therefore include hardening beyond the three user-facing customizations; they must remain regression-tested and are candidates for later surgical reduction."
  },
  "verification": {
    "targeted": "17 test files / 197 tests passed",
    "full_suite_comparison": {
      "reset_branch_failed_assertions": 97,
      "vanilla_upstream_failed_assertions": 100,
      "old_fork_main_failed_assertions": 54,
      "new_regressions_vs_vanilla_upstream": 0,
      "behaviors_not_carried_from_old_fork": 46
    },
    "live_production": "Eight direct Codex probes passed: chat and Responses, streaming and non-streaming, on gpt-5.6-terra and gpt-6-astra. No InvalidStreamResponseError or runtime TypeError/ReferenceError was observed.",
    "live_claude_prefill": "Verified live on 2026-09-10 against the running production gateway. The previously recorded revoked-OAuth blocker no longer applies: the Claude connection refreshes successfully and serves traffic. Direct probes covering a trailing assistant text turn, a contentless trailing assistant, an interrupted function_call, and a reasoning-only tail all returned HTTP 200 through both /v1/messages and /v1/responses.",
    "known_defects": "Production logs showed HTTP 400 \"does not support assistant message prefill\" on the openai-responses to claude combo path. Root cause: applyAssistantPrefillPolicy checked messages.at(-1) once, so dropping a contentless trailing assistant could expose another assistant turn, and a body whose only turn was a contentless assistant was left with an empty messages array that prepareClaudeRequest then dereferenced. Fixed on branch fix/claude-prefill-invariant-loop (pull request 2); not yet deployed."
  },
  "maintenance_rules": [
    "Compare this manifest and its counts with git diff --name-status against upstream_sha before release.",
    "Do not restore retired Responses/SSE conversion seams without isolated regression tests covering content loss, duplicate content, stream and non-stream parity, and InvalidStreamResponseError.",
    "Keep additive helpers additive where possible; reduce whole-file dependency carryovers only with feature-level and baseline tests.",
    "Update upstream.ref, upstream.sha, diff counts, retained paths, tests, and known behavior losses together on every upstream reset."
  ],
  "restored_to_upstream_seams": {
    "note": "Fork modifications to these seams were dropped and the files now match upstream byte-for-byte. They are live upstream code \u2014 do not delete them.",
    "paths": [
      "open-sse/handlers/chatCore/nonStreamingHandler.js",
      "open-sse/handlers/chatCore/sseToJsonHandler.js",
      "open-sse/handlers/chatCore/streamingHandler.js",
      "open-sse/handlers/responsesHandler.js",
      "open-sse/executors/codex.js",
      "open-sse/transformer/responsesTransformer.js",
      "open-sse/transformer/streamToJsonConverter.js"
    ]
  },
  "deleted_fork_only_files": {
    "note": "Fork inventions with no upstream counterpart, removed by the reset.",
    "paths": [
      "open-sse/utils/streamContract.js"
    ]
  },
  "review_fixes": {
    "note": "Defects found by independent review of the reset branch and fixed in place.",
    "items": [
      "chatCore now maps ToolCompatibilityError to HTTP 400 instead of letting it escape as an opaque 500 that a combo would retry.",
      "chatCore passes raw client headers to normalizeClaudePassthrough so x-9router-assistant-prefill: preserve works on native passthrough.",
      "The prefill policy keeps a trailing turn holding signed or redacted reasoning instead of silently dropping unregenerable provider history.",
      "GET /api/contributor-admin/invites now enforces the same-origin check its mutating siblings use.",
      "Both contributor routes use the bounded JSON reader with an explicit byte cap and reject malformed bodies with 400 rather than 500.",
      "The invite route no longer returns raw internal error messages.",
      "The contributor session secret only self-creates when genuinely absent or empty, uses exclusive create, and propagates other IO faults instead of rotating the live secret.",
      "Combo import rejects a zero-model combo that would persist but never route.",
      "parseRetryDeadline again accepts a numeric epoch retryAfter from the JSON error body, restoring the Retry-After signal.",
      "The import/export route imports @/lib/db/index.js instead of the localDb compat shim."
    ]
  },
  "known_inert_carryovers": {
    "note": "Retained whole-file dependencies whose largest new blocks have no production caller, because their call sites remain at upstream. Tested in isolation but unreachable; candidates for symbol-level reduction or removal in a follow-up.",
    "items": [
      "src/sse/services/auth.js account-mutation ordering (beginAccountMutationAttempt and friends) has no callers.",
      "open-sse/services/combo.js abort/signal plumbing is unreachable because src/sse/handlers/ remains at upstream arity.",
      "open-sse/providers/capabilities.js getProviderModalities has no implementer.",
      "src/lib/oauth/utils/server.js records createdAt on pending sessions but nothing reads or sweeps it."
    ]
  }
}
